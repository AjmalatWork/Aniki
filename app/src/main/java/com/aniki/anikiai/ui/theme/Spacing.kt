package com.aniki.anikiai.ui.theme

import androidx.compose.ui.unit.Dp
import androidx.compose.ui.unit.dp

/**
 * Shared vertical-rhythm scale, introduced with the Feed bottom-zone spacing pass ("polish pass 2"
 * item 3) so stacked elements (hint, text, source, tags) pull from one scale instead of each
 * picking its own one-off gap.
 */
object Spacing {
    val xs: Dp = 4.dp
    val sm: Dp = 8.dp
    val md: Dp = 12.dp
    val lg: Dp = 16.dp
    val xl: Dp = 24.dp
}
